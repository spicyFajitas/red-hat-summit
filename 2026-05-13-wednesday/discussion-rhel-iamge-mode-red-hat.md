# RHEL Meeting Image Mode

Ansible - instalklking software needs to shift to containerfile

Ansible is better for spoecific host imprinting. Anythign that lives in /etc

kickstart callbacks - proivisioning template using activation keys

registration in post, once in AAP inventory, then apply provisioning changes

kickstart callback triggers job for making changes

current idea is assigning two vNICs during VM creation and then running cleanup once the VM is fully provisioned

bootc timer for updates, push with Ansible

bcvk mention - going into extensions first and then rhel

tekton & argoCD, this could be leveraged in the same manner

hierarchy of code folders
    bottom level hardening
    standard operating environment
    make application specific images from base hardened image